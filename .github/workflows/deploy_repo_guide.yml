name: Publish documentation generated by repo-guide

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: wolfmanstout/deploy-repo-guide@bc2a7ff1f58d3cffa481826186ec2ba3d3283491
        with:
          gemini-key: ${{ secrets.LLM_GEMINI_KEY }}
